[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "muton"
version = "0.1.0"
description = "Multimodal conversation assistant research code."
requires-python = ">=3.10"
